import { MainAuthor } from "@/components/landing";
import { initServerPages } from "@/utils";
import { Metadata } from "next";

const Author = () => {
    return (
        <>
            <MainAuthor />
        </>
    );
};

export default Author;
