// import { MainTeachers } from "@/components/landing";
import { MainTeachers } from "@/components/landing/teacher";
import { initServerPages } from "@/utils";
import { Metadata } from "next";

const Teacher = () => {
    return (
        <>
            <MainTeachers />
        </>
    );
};

export default Teacher;
