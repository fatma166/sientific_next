import { MainStudents } from "@/components/landing/students";
import { initServerPages } from "@/utils";
import { Metadata } from "next";

const Teacher = () => {
    return (
        <>
            <MainStudents />
        </>
    );
};

export default Teacher;
